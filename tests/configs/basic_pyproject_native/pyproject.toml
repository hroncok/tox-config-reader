[build-system]
requires = ["flit_core>=3.4"]
build-backend = "flit_core.buildapi"

[project]
name = "mypackage"

[tool.tox]
requires = ["tox>=4.0"]
env_list = ["py39", "py310"]

[tool.tox.env_run_base]
deps = ["pytest"]
commands = [["pytest"]]

