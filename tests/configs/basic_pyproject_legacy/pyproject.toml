[build-system]
requires = ["flit_core>=3.4"]
build-backend = "flit_core.buildapi"

[project]
name = "mypackage"

[tool.tox]
legacy_tox_ini = """
[tox]
env_list = py39, py310

[testenv]
deps = pytest
commands = pytest tests
"""

